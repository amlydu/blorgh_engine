<h3>New Comment</h3>

<%= form_for [@article, @article.comments.build], html: {class: "create-comment"} do |f| %>
  <p>
    <%= f.text_field :title %>
    <%= f.text_area :text %>
  </p>
  <%= f.submit "Create a Comment" %>
<% end %>
